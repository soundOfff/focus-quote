# Build-time configuration — copy to .env (never commit .env).
# Baked into the extension bundle at build time via Vite define.

# Where the FocusQuote backend lives. In dev, point at your local server.
# In prod, set this to your hosted API URL (e.g. https://api.focusquote.app).
API_BASE_URL=http://localhost:3000
