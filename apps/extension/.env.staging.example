# Staging build template for the FocusQuote extension.
#
# Copy to `.env.staging`, then build with:
#   npm run build -- --mode staging
# Vite's `loadEnv("staging", ...)` will pick this file up and inline
# `API_BASE_URL` via `define`.

API_BASE_URL=https://focusquote-staging.example.com
